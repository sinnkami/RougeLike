class WindowBase {
  constructor(item, map, log, header, menu, quit, status) {
    this.item = item;
    this.map = map;
    this.log = log;
    this.header = header;
    this.menu = menu;
    this.quit = quit;
    this.status = status;

  }
}
